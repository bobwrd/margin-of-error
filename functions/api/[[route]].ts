// SUPERSEDED — this file is no longer used.
//
// Cloudflare folded Pages into Workers, so the Pages `functions/` file-based
// routing convention was replaced by a single Worker entry point. The live
// implementation now lives in `worker.ts` at the repo root (configured via
// `main` in wrangler.toml). This file is kept only to avoid a dangling import;
// it is not bundled or deployed.

export {};
