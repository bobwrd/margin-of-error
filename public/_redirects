# Intentionally empty.
# SPA fallback (serving index.html for client-side routes) is handled by
# `not_found_handling = "single-page-application"` in wrangler.toml.
# Do NOT add a `/*  /index.html  200` rule here — under the Workers static-
# assets model that creates an infinite-loop error at deploy time.
